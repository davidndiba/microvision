import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const AppFooter = () => {
  return <Footer style={{ textAlign: 'center' }}>©2024 Your Company</Footer>;
};

export default AppFooter;
